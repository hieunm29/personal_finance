# Checklist Triển khai — Dashboard (US-DASH-01 → 07)
Module 4 — Phase 1 MVP (DASH-01→05) + Phase 2 Placeholder (DASH-06→07)
Tham chiếu: 2d.userstories_dashboard.md, 3b.tech_structure.md

---

## Phần 0 — Infrastructure chung

### 0.1. Shared — Types (`packages/shared/src/types/index.ts`)
- [x] Thêm `TopExpenseCategory` — { categoryId: string | null, categoryName: string, categoryIcon: string | null, categoryColor: string | null, amount: number, percentage: number }
- [x] Thêm `MonthlyChartPoint` — { month: string (YYYY-MM), income: number, expense: number }
- [x] Thêm `BudgetProgressItem` — { categoryId: string, categoryName: string, limit: number, spent: number }
- [x] Thêm `DashboardData`:
  ```typescript
  interface DashboardData {
    totalIncome: number;           // Tháng hiện tại (cents)
    totalExpense: number;          // Tháng hiện tại (cents)
    netAmount: number;             // totalIncome - totalExpense
    prevMonthIncome: number;       // Tháng trước (cents)
    prevMonthExpense: number;      // Tháng trước (cents)
    totalBalance: number;          // Tổng số dư tất cả wallets (cents)
    netWorth: number | null;       // Phase 2
    recentTransactions: TransactionWithRelations[];
    topExpenseCategories: TopExpenseCategory[];
    monthlyChart: MonthlyChartPoint[];
    budgetProgress: BudgetProgressItem[] | null; // Phase 2
  }
  ```

### 0.2. Shared — Constants (`packages/shared/src/constants/index.ts`)
- [x] Verify `QUERY_KEYS.dashboard` đã có — nếu chưa, thêm `dashboard: ['dashboard'] as const`

### 0.3. Backend — Service (`apps/server/src/services/dashboardService.ts`) — tạo mới
- [x] Import: `db`, `transactions`, `wallets`, `categories`, `userProfiles` từ schema; `eq, and, gte, lte, desc, sql` từ drizzle-orm
- [x] Helper `getProfileId(authUserId: string): string`
- [x] Helper `getMonthRange(month: string): { start: string; end: string }`
- [x] Export `getDashboardData(authUserId: string, month: string): DashboardData`

### 0.4. Backend — Route (`apps/server/src/routes/dashboard.ts`) — tạo mới
- [x] `GET /` với query schema: `z.object({ month: z.string().regex(/^\d{4}-\d{2}$/).optional() })`
- [x] Xác định `currentMonth` = `new Date().toLocaleDateString('sv').substring(0, 7)`
- [x] Gọi `getDashboardData(authUserId, month ?? currentMonth)`
- [x] Return `c.json({ data: result })`

### 0.5. Backend — Mount (`apps/server/src/index.ts`)
- [x] Thêm `import dashboardRoutes from './routes/dashboard'`
- [x] Thêm `app.route('/api/dashboard', dashboardRoutes)` sau auth middleware

### 0.6. Frontend — Hook (`apps/web/src/hooks/useDashboard.ts`) — tạo mới
- [x] `useDashboard(month?: string)` với queryKey, queryFn, staleTime: 30_000

---

## Phần 1 — US-DASH-01: Tổng quan tài chính tháng

### Backend (`dashboardService.ts`) — monthSummary queries
- [x] Tính `prevMonth`
- [x] `totalIncome` (tháng hiện tại)
- [x] `totalExpense`
- [x] `prevMonthIncome`
- [x] `prevMonthExpense`
- [x] `netAmount = totalIncome - totalExpense`

### Frontend (`apps/web/src/components/dashboard/MonthSummaryCards.tsx`) — tạo mới
- [x] Props: `{ totalIncome, totalExpense, netAmount, prevMonthIncome, prevMonthExpense, currency: string }`
- [x] Hàm tính % thay đổi
- [x] Card "Tổng thu" (xanh lá) + Badge ↑/↓/%
- [x] Card "Tổng chi" (đỏ) + Badge %
- [x] Card "Chênh lệch" xanh/đỏ theo dấu
- [x] Skeleton loading state (animate-pulse)

### Acceptance Tests (manual)
- [x] Dashboard là trang mặc định sau đăng nhập (`/` → DashboardPage)
- [x] 3 cards hiển thị đúng số liệu tháng hiện tại
- [x] Chênh lệch màu đỏ khi âm
- [x] Badge "—" khi không có dữ liệu tháng trước
- [x] Thêm giao dịch → invalidate `QUERY_KEYS.dashboard` → cards cập nhật (invalidation đã có trong useTransactions)

---

## Phần 2 — US-DASH-02: Số dư hiện tại

### Backend (`dashboardService.ts`) — totalBalance query
- [x] `totalBalance = SUM(wallet.initialBalance) + SUM(all income txns) - SUM(all expense txns)` — toàn thời gian

### Frontend (`apps/web/src/components/dashboard/WalletBalanceCard.tsx`) — tạo mới
- [x] Props: `{ totalBalance: number, currency: string }`
- [x] Hiển thị số lớn nổi bật
- [x] Label "Tổng số dư", sub-label "Tất cả tài khoản / ví"
- [x] Màu xanh khi `>= 0`, đỏ khi âm
- [x] Skeleton loading state

### Acceptance Tests (manual)
- [x] Số dư đúng = walletInit + allIncome - allExpense

---

## Phần 3 — US-DASH-03: Giao dịch gần đây

### Backend (`dashboardService.ts`) — recentTransactions query
- [x] JOIN transactions với categories + wallets, order by date DESC + createdAt DESC, LIMIT 5

### Frontend (`apps/web/src/components/dashboard/RecentTransactionsList.tsx`) — tạo mới
- [x] Props: `{ transactions, currency, onEditTransaction }`
- [x] Mỗi row: colored circle, tên category, note, số tiền màu đúng, ngày
- [x] Transfer type hiển thị màu xám (bugfix từ code review)
- [x] Click row → `onEditTransaction(txn.id)`
- [x] Nút "Xem tất cả →" → `/transactions`
- [x] Empty state

### DashboardPage — wiring edit modal
- [x] State `editTxnId` + tìm `editTxn` từ `recentTransactions`
- [x] Modal với `TransactionForm` mode="edit" + defaultValues pre-filled

### Acceptance Tests (manual)
- [x] 5 giao dịch mới nhất hiển thị đúng
- [x] Click giao dịch → modal sửa mở, pre-filled đúng dữ liệu
- [x] "Xem tất cả" → /transactions ✓

---

## Phần 4 — US-DASH-04: Biểu đồ thu chi 6 tháng

### Backend (`dashboardService.ts`) — monthlyChart query
- [x] Tính `sixMonthsAgo`
- [x] Seed Map với 6 tháng zeros
- [x] Aggregate transactions vào Map
- [x] Sort ASC, map sang `MonthlyChartPoint[]`

### Frontend (`apps/web/src/components/dashboard/MonthlyChart.tsx`) — tạo mới
- [x] ChartJS.register ở top-level file
- [x] Props: `{ data, currency }`
- [x] Labels MM/YYYY, datasets Thu nhập xanh / Chi tiêu đỏ
- [x] Y axis tick + Tooltip callback dùng formatCurrency
- [x] Wrapper `h-64 md:h-80`, `responsive: true, maintainAspectRatio: false`

### Acceptance Tests (manual)
- [x] Chart hiển thị 6 tháng, cột cuối = tháng hiện tại
- [x] Hover tooltip đúng format
- [x] Tháng không có giao dịch → cột 0

---

## Phần 5 — US-DASH-05: Top 5 khoản chi lớn nhất

### Backend (`dashboardService.ts`) — topExpenseCategories query
- [x] GROUP BY categoryId, SUM DESC, LIMIT 5
- [x] Map sang `TopExpenseCategory[]` với percentage

### Frontend (`apps/web/src/components/dashboard/TopExpenseCategoriesList.tsx`) — tạo mới
- [x] Props: `{ categories, currency, month }`
- [x] Mỗi row: colored circle, tên, amount, badge %, progress bar
- [x] Click → navigate với URLSearchParams đúng (bugfix: omit categoryId khi null)
- [x] Empty state

### Acceptance Tests (manual)
- [x] Top 5 danh mục sort DESC
- [x] % đúng, progress bar tỷ lệ
- [x] Click row → /transactions filter đúng ✓

---

## Phần 6 — US-DASH-06: Tiến độ ngân sách (Phase 2 Placeholder)

### Backend
- [x] `budgetProgress = null`

### Frontend (`apps/web/src/components/dashboard/BudgetProgressCard.tsx`) — tạo mới
- [x] Placeholder card với CTA "Thiết lập ngân sách"

### Acceptance Tests (manual)
- [x] Card placeholder hiển thị đúng

---

## Phần 7 — US-DASH-07: Net Worth (Phase 2 Placeholder)

### Backend
- [x] `netWorth = null`

### Frontend (inline trong `DashboardPage.tsx`)
- [x] Placeholder text khi `netWorth === null`

### Acceptance Tests (manual)
- [x] Placeholder hiển thị

---

## Phần 8 — DashboardPage layout & wiring

### `apps/web/src/pages/DashboardPage.tsx`
- [x] useDashboard() hook
- [x] useQuery profile để lấy currency
- [x] Loading skeleton
- [x] Grid layout responsive (lg:col-span-2 + 1/3)
- [x] Edit modal wiring với defaultValues

### Cache invalidation
- [x] `useCreateTransaction`: invalidates dashboard ✓ (đã có)
- [x] `useUpdateTransaction`: invalidates dashboard ✓ (đã có)
- [x] `useDeleteTransaction`: invalidates dashboard ✓ (đã có)

### Acceptance Tests (manual)
- [x] `/` sau đăng nhập → DashboardPage load đầy đủ
- [x] Layout responsive
- [x] Loading skeleton
- [x] Bar chart 6 tháng
- [x] Top 5 categories + drill-down
- [x] Recent transactions + edit modal
- [x] `bun run typecheck` — 0 errors ✓

---

## Tổng hợp Files đã tạo/sửa

### Files MỚI — Backend
| # | File | Trạng thái |
|:--|:-----|:---------|
| 1 | `apps/server/src/services/dashboardService.ts` | ✅ |
| 2 | `apps/server/src/routes/dashboard.ts` | ✅ |

### Files ĐÃ SỬA — Backend
| # | File | Trạng thái |
|:--|:-----|:---------|
| 3 | `apps/server/src/index.ts` | ✅ |

### Files ĐÃ SỬA — Shared
| # | File | Trạng thái |
|:--|:-----|:---------|
| 4 | `packages/shared/src/types/index.ts` | ✅ |
| 5 | `packages/shared/src/constants/index.ts` | ✅ (đã có) |

### Files MỚI — Frontend
| # | File | Trạng thái |
|:--|:-----|:---------|
| 6 | `apps/web/src/hooks/useDashboard.ts` | ✅ |
| 7 | `apps/web/src/components/dashboard/MonthSummaryCards.tsx` | ✅ |
| 8 | `apps/web/src/components/dashboard/WalletBalanceCard.tsx` | ✅ |
| 9 | `apps/web/src/components/dashboard/RecentTransactionsList.tsx` | ✅ |
| 10 | `apps/web/src/components/dashboard/MonthlyChart.tsx` | ✅ |
| 11 | `apps/web/src/components/dashboard/TopExpenseCategoriesList.tsx` | ✅ |
| 12 | `apps/web/src/components/dashboard/BudgetProgressCard.tsx` | ✅ |

### Files ĐÃ SỬA — Frontend
| # | File | Trạng thái |
|:--|:-----|:---------|
| 13 | `apps/web/src/pages/DashboardPage.tsx` | ✅ |
| 14 | `apps/web/src/hooks/useTransactions.ts` | ✅ (đã có invalidation) |
